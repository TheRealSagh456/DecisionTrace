import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        borderWidth: 1,
        borderColor: "#d1d5db",
        paddingHorizontal: 6,
        height: "auto",
        width: "100%",
        flexDirection: "row",
        alignItems: "center"
    }
})